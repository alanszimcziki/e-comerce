spring:
  application:
    name: e-comerce
    data:
      host: ${MONGO_HOST}
      port: 27017
      database: e-comerce
    redis:
      password: ${PASSWORD_REDIS}
      port: 6379
      host: ${REDIS_HOST}
  cache:
    redis:
      time-to-live: 60000
basket:
  client:
    platzi: https://api.escuelajs.co/api/v1


